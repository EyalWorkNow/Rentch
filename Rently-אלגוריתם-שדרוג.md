# Rently — שדרוג האלגוריתם: תוכנית מפורטת (הצעה)

מסמך המשך ל-`Rently-אלגוריתם-מפורט.md`. מרכז תוכנית קונקרטית לשדרוג אלגוריתם החיפוש וההתאמה האישית: דיוק חיפוש עבור המשתמש, בניית פרופיל-משתמש חי דרך אריק, קפיצת-מדרגה בעוצמה החישובית של הדירוג, ושכבת איסוף/מיצוי דאטה למכירה. כולל שמות שדות/פונקציות מוצעים בהתאם למוסכמות הקוד הקיימות (`rentch-*`, `camelCase`, function-calling בסגנון אריק), טבלאות השוואה בין המצב הנוכחי למוצע, ומפת-דרכים מדורגת מהצעד הזול ביותר ועד לארוך-טווח.

---

# אבחון: מה מגביל את הדיוק היום

| # | הפער | המקור במסמך המקורי |
|---|---|---|
| 1 | הדירוג (שלב 4) הוא נוסחה ליניארית עם משקלים **זהים לכל משתמש** | טבלת שלב 4 — 6 משקלים קבועים |
| 2 | `vibe` נאסף ב-`PropertySearchCriteria` (שלב 1) אך לא מופיע בנוסחת הניקוד בכלל | השוואת שלב 1 מול טבלת שלב 4 |
| 3 | מאגר המועמדים מוגבל ל-100–200 רשומות לפני הדירוג | `Query.limit(100)`, `search_listings` עם `Limit:200` |
| 4 | `rentch-search-log` כבר אוסף בדיוק את מה שצריך לאימון ranker — בלי consumer שקורא ממנו | שלב 5 + טבלת "מה שעדיין לא" |
| 5 | אין שכבת-זיכרון בין שיחות אריק — כל שיחה מתחילה מאפס | "אריק RAG + זיכרון" מופיע רק כ-stage 2 מתוכנן |
| 6 | 3 נקודות המגע (אריק / חיפוש NL / שאל את Rently) לא מתוארות כחולקות מידע | אין תיאור shared state במסמך |
| 7 | `popularity_prior` גלובלי — אין "משתמשים כמוך אהבו גם" | מבוסס like-rate כללי, לא per-user |

ארבע ההמלצות שלהלן (חלקים א'–ד') הן בעצם ארבע דרכים לסגור את שבעת הפערים האלה. תרשים-העל של הפתרון:

```
נקודות מגע (אריק · חיפוש NL · שאל את Rently)
        ↓
פרופיל משתמש חי (העדפות + embedding אישי)          ← חלק ב'
        ↓
איתור מועמדים מורחב (סינון + embeddings + collaborative)   ← חלק א'.1
        ↓
דירוג נלמד — LightGBM (עשרות מאפיינים, לא 6)        ← חלק א'.2–3, חלק ג'
        ↓
תוצאות מדורגות אישית
        ↺  כל תוצאה, קליק, סוויפ, שמירה ופנייה חוזרים ומעדכנים
           את הפרופיל ואת rentch-search-log
```

---

# חלק א' — דיוק החיפוש: משלוש שכבות לצינור אמיתי

## 1. איתור מועמדים — לא רק city + limit(100)

לשמור על הסינון הקשיח הקיים (הוא זול וטוב) אבל להוסיף לו שני מקורות מועמדים שרצים במקביל ומתמזגים:

| מקור | איך | מה הוא תופס שהיום נופל |
|---|---|---|
| סינון מובנה (קיים) | `Query.equal('city',…)` | עיר/מחיר/חדרים מדויקים |
| **kNN על embeddings** | embedding לתיאור+תמונות מול embedding לשאילתה/למשתמש | "דירה עם אווירה כפרית", "שקטה בלילה" — בדיוק מה ש-`vibe` היום אוסף ולא עושה איתו כלום |
| **collaborative** | דירות שמשתמשים עם פרופיל דומה אהבו | "משתמשים כמוך התעניינו גם ב-X" — לא קיים היום |

לבטל את התקרה השרירותית (100/200) ולתת לשלב האיתור להביא כמה מאות עד כ-2,000 מועמדים — שהדירוג, לא האחזור, יצמצם.

## 2. הרחבת וקטור המאפיינים

| משפחת-מאפיינים | דוגמה קונקרטית | למה זה לא קיים היום |
|---|---|---|
| **משקל-מאפיין אישי** | לא "יש/אין חניה" אלא `user_weight[חניה] × has(חניה)`, כש-`user_weight` נלמד מהיסטוריית סוויפים/שיחות | `tag_overlap` היום הוא hit/requested גלובלי |
| **התאמת-תקציב אישית** | לא "קרוב לאמצע התקציב" אלא עקומת-אלסטיות אישית — כמה המשתמש הזה בפועל "מותח" תקציב | `price_fit` היום זהה לכולם |
| **התאמת-שכונה אישית** | cosine בין וקטור תת-הציונים הקיים (safety/walkability/schools/transit/green — כבר יש ב-6.2 במסמך המקורי) לבין משקלים אישיים | תת-הציונים מחושבים אבל לא מותאמים לפרופיל |
| **דמיון סמנטי** | cosine(embedding-משתמש, embedding-דירה) | 0 embeddings היום |
| **ציון collaborative** | דמיון לפרופילים שאהבו את אותה דירה | לא קיים |
| **גיוון (MMR)** | לא לתת ל-12 תוצאות להיות "אותה דירה" | `explore` היום הוא רק cold-start, לא anti-redundancy |

תוצאה: מ-6 מאפיינים ל-40–80 (תלוי בפירוק), רובם עכשיו תלויי-משתמש ולא גלובליים.

## 3. מליניארי-קבוע למודל נלמד

המסמך המקורי כבר מסמן נכון את הכיוון (הלוג = "סט האימון לranker העתידי LightGBM lambdarank"). כדי שזה באמת יעבוד צריך שני תיקונים:

1. **הלוג חייב labels, לא רק impressions.** impression לבד הוא רק "הוצג". צריך שלכל `impressionId` יהיה מקושר קליק/שמירה/פנייה (positive) מול "הוצג ולא נלחץ" (negative implicit). אם זה כבר קיים — מצוין; אם לא, זו תוספת-שדה קטנה עם החזר גדול.
2. **cold start למודל עצמו.** LightGBM לומד היטב רק אחרי כמה אלפי אינטראקציות. עד אז — middle-ground: משקלים דינמיים **per-cohort** (משפחות / יחידים / סטודנטים, נגזר מסוויפ+שיחה) במקום גלובלי-קבוע. קפיצה משמעותית בלי לחכות לנפח-דאטה גדול.

חשוב לא פחות: **A/B בפועל** לפני שמחליפים ניקוד. כל האימותים במסמך המקורי הם "אומת חי: X" נקודתי — טוב לצינורות דאטה כמו geocode, לא מספיק ל-relevance, ששם רק מדידה סטטיסטית על אוכלוסייה אומרת אם השינוי שיפר משהו.

---

# חלק ב' — פרופיל משתמש דרך אריק

אריק כבר מדבר עברית חופשית ועושה function-calling מובנה — בדיוק התשתית הדרושה. היום ה-tool-loop חסום ל-5 hops בתוך שיחה בודדת (הגנה טובה מפני תקיעה), אבל שום דבר לא נשמר *בין* שיחות: "אריק RAG + זיכרון" רשום כ-stage 2 מתוכנן, לא קיים.

## 1. טבלת `rentch-user-profiles` — הצעת סכמה

| שדה | סוג | מקור | הערה |
|---|---|---|---|
| `explicit.cityPrefs` | `[{city, weight, updatedAt}]` | שיחות + onboarding | recency-decay כמו `recency` הקיים |
| `explicit.priceRange` | `{min, max, softness}` | היסטוריית NL search | softness = כמה חרג בעבר מהתקציב שהצהיר |
| `implicit.amenityWeights` | `map<amenity, 0..1>` | ניתוח batch לילי של סטים positive/negative מהסוויפים | לא boolean — משקל |
| `implicit.neighborhoodWeights` | `{safety, walkability, schools, transit, green}` | קורלציה בין תת-הציונים הקיימים לדירות שאהב/דחה | ממנף דאטה שכבר קיים ב-6.2 |
| `userEmbedding` | `vector[d]` | ממוצע-משוקלל של embeddings לדירות שאהב, פחות שדחה | "מגדל-משתמש" מול "מגדל-דירה" (two-tower) |
| `lifestyleFlags` | `{hasPets, hasChildren, wfh, …}` | חילוץ משיחות אריק (`responseSchema` — אותה שיטה שכבר עובדת ב-`/assistant/extract`) | + confidence לכל flag |
| `interactionSummary` | `{swipes, saves, contacts, asks}` | אגרגציית לוג | |

## 2. מנגנון עדכון — כלי שלישי בלולאה

כלי חדש לצד `search_listings` ו-`create_property`, באותה לולאה ואותו grounding rule:

```
update_user_profile({ field, value, confidence, source })
```

רץ async (לא חוסם UI, בדיוק כמו `Promise.race` ב-`enrichListingOnCreate`) — בסוף כל session, ואחרי X סוויפים.

## 3. חיבור בין 3 נקודות המגע

היום "שאל את Rently" קבור בהקשר-דירה בודד. משתמש ששואל "זה שקט בלילה?" על 5 דירות שונות — זה סיגנל חזק (רגישות לרעש) שנשרף בסוף השיחה במקום לזרום ל-profile. אותו דבר לגבי הניסוח החופשי ב-NL search, לא רק השדות המחולצים ממנו.

## 4. שקיפות = גם UX וגם דאטה איכותי

מסך "מה Rently למד עליי" עם אפשרות לתקן משרת שלוש מטרות בבת אחת: אמון המשתמש, תאימות רגולטורית (ראה חלק ד'), ותיקון ידני הוא label איכותי בהרבה מניחוש.

## 5. Cold start

2–3 שאלות קצרות ב-onboarding + סבב סוויפים מגוון בהתחלה, כדי ש-`userEmbedding` יתכנס מהר. והגבלה חשובה בכיוון ההפוך: מקסימום שאלת-פרופיל אחת-שתיים לכל session של אריק, לא תשאול — אחרת זה פוגע ב-UX.

---

# חלק ג' — קפיצת מדרגה חישובית

| ממד | היום | מוצע |
|---|---|---|
| גודל מאגר-מועמדים | ~100–200 (`Query.limit`) | 500–2,000 (סינון + kNN + collaborative) |
| מס' מאפיינים למועמד | 6 | 40–80 |
| משקלים | סט גלובלי אחד לכולם | נלמדים, per-user/per-cohort |
| מבנה המודל | דוט-פרודקט ליניארי | GBDT (LightGBM, מאות עצים) |
| embeddings | 0 | לכל דירה + כל משתמש + כל שאילתה |

**למה "עשרות מונים" ולא סתם מספר יפה:** מאגר גדול פי ~10 (100→1,000) כפול מספר-מאפיינים גדול פי ~10 (6→60) כבר נותן בסביבות פי 100 "עבודת-חישוב אינפורמטיבית" לחיפוש בודד — ולפני שסופרים שהחישוב עצמו הופך מדוט-פרודקט זול למודל-עצים לא-ליניארי (יקר משמעותית יותר per-evaluation), ולפני batch jobs לילי לחישוב embeddings שהיום פשוט לא קיימים. זו הערכה כיוונית, לא בנצ'מארק מדוד — אבל "כמה עשרות מונים" הוא תיאור זהיר של הקפיצה, לא מוגזם.

## תשתית קונקרטית — נשארים ב-AWS, לא מוסיפים ספק

**וקטורים:** Amazon S3 Vectors — הגיע ל-GA בדצמבר 2025, תומך באחסון ושאילתה על עד כ-2 מיליארד וקטורים לאינדקס בזמן תגובה של כ-100 מילישניות, ומוזיל את עלות האחסון והשאילתה על וקטורים בעד 90% לעומת מסדי-נתונים ייעודיים לוקטורים. מתחבר טבעי ל-S3 שכבר בסטאק (לתמונות) — בלי ספק חדש ובלי ניהול cluster, ומשתלב גם עם OpenSearch אם בהמשך יידרש hybrid search מתקדם יותר.

**Embeddings:** להישאר עם Gemini (כבר בשימוש ל-LLM ול-smart-tags). Gemini Embedding 2 (הגיע ל-GA באפריל 2026) הוא מודל embedding מולטימודלי שממפה טקסט, תמונות, וידאו, אודיו ומסמכים למרחב embedding אחד, ותומך בקלט של עד 8,192 טוקני טקסט ו-6 תמונות בבקשה אחת. זה בדיוק מתאים לקריאת ה-multimodal הקיימת ב-smart-tags (כותרת+תיאור+עד 4 תמונות) — אותה קריאה יכולה להחזיר גם embedding וגם תגיות, בלי API נוסף.

**Feature store:** בשלב ראשון — עוד טבלת DynamoDB (`rentch-user-profiles`, `rentch-listing-features`), לא פתרון ייעודי כמו Feast — overkill בגודל הנוכחי.

**Batch jobs:** EventBridge Scheduled Rule → Lambda, תבנית שכבר קיימת בסטאק.

**אימון:** LightGBM כ-batch job (Lambda/Fargate) על export מ-`rentch-search-log`; ה-inference קל מספיק לרוץ בתוך Lambda קיים.

---

# חלק ד' — איסוף ומיצוי דאטה למכירה

## 1. מה כדאי לתעד שהיום לא (או לא ברור שכן)

- **latency-to-decision בסוויפ** — כמה זמן לקח להחליט (החלטה מהירה = איתות חזק; היסוס = ambivalence, שגם הוא סיגנל)
- **טקסט חופשי גולמי מ-NL search**, לא רק השדות המחולצים — הניסוח עצמו הוא דאטה למגמות-שוק
- **רגעי נטישה** (תהליך שהתחיל ולא הושלם) — סיגנל שלילי חשוב לא פחות מחיובי
- **שאלות "שאל את Rently"** כדאטה בפני עצמו, לא רק כקלט לפרופיל (ראה חלק ב'.3)

## 2. מהפרופיל הגולמי למוצר מכירה — לא אותו דבר

הנקודה המרכזית: הפרופיל העשיר שתואר בחלק ב' צריך להתקיים קודם כל בשביל המוצר עצמו — חיפוש טוב יותר הוא הסיבה שמצדיקה לאסוף אותו ושבגללה משתמש בכלל ייתן לזה אמון. המכירה בפועל היא שכבה נפרדת, בנויה מעליו:

```
פרופיל + לוג אינטראקציות (ברמת משתמש בודד)
        ↓
אנונימיזציה ואגרגציה (רף מינימום למקטע)
        ↓
מוצרי דאטה לשוק B2B (ביקוש · ערך-מאפיין · לידים)
        ↓
לקוחות: יזמים ומשקיעים ועיריות (בשילוב Obrik — דאטה מרחבי)
```

זה גם מהלך עסקי חכם יותר, לא רק "בטוח יותר": יזם נדל"ן לא רוצה את היסטוריית-הסוויפים של יוסי כהן — הוא רוצה "ביקוש למרפסות + חניה בין גילאי 25–35 ברמת-גן עלה 18% ברבעון". דאטה מאונדם ומאונדק שווה יותר למי שקונה, לא פחות.

מוצרים קונקרטיים (כולם ברמת מקטע, לא פרט):

- **מפות ביקוש** לפי שכונה/עיר/סוג-דירה — ליזמים, משקיעים, ואפילו עיריות לתכנון
- **מדד ערך-מאפיין** — כמה renters "משלמים" בהתעניינות (לא רק במחיר) עבור חניה/מרפסת/שיפוץ, לפי אזור וקוהורט — שמאים ומלווים ישלמו על זה
- **עקומות אלסטיות מחיר** לפי סגמנט — למשכירים ולמשקיעים מוסדיים
- **lead-gen באישור מפורש** (המשתמש בוחר לשתף פרופיל עם משכיר/יזם תואם) — המודל הכי נקי מבחינת consent, סטנדרטי בתעשיית נדל"ן/משכנתאות

**החיבור ל-Obrik:** Obrik כבר אוסף את צד ה*היצע* — דאטה מרחבי-3D, מה הדירות נראות פיזית. מוצר-הביקוש שתואר כאן הוא הצד השני של אותו מטבע — מה renters *באמת* רוצים, מאותה פלטפורמה. שילוב היצע+ביקוש מאותו מקור הוא הצעה שקשה למתחרה לשכפל, כי אף אחד אחר לא יושב על שני הצדדים בו-זמנית.

## 3. המצב המשפטי — לא ייעוץ, אבל חשוב לדעת לפני שבונים

תיקון 13 לחוק הגנת הפרטיות נכנס לתוקף ב-14 באוגוסט 2025 ומיישר את המשטר הישראלי קרוב יותר לתקנים גלובליים כמו GDPR. שלוש נקודות רלוונטיות ישירות לתוכנית כאן:

1. **"data brokers" הן קטגוריה מוגדרת עם חובות משלה.** גופים ציבוריים, data brokers, ארגונים שעיקר פעילותם עיבוד מידע רגיש-במיוחד, וגם כאלה שעורכים מעקב שיטתי — כולם מחויבים במינוי קצין הגנת פרטיות (DPO). אם המכירה תגדל להיקף משמעותי, כדאי לבדוק מול עו"ד אם רנטלי נכנסת לקטגוריה הזו.
2. **הסכמה חייבת להיות גרנולרית, לא גורפת.** ההסכמה חייבת להיות מפורשת, מתועדת וממוקדת-נושא; הסכמה גורפת אחת לכל השימושים כבר לא מספיקה, ומדיניות הפרטיות צריכה לפרט מה נאסף, למה, מה הסיכון, ולמי זה מועבר.
3. **"data creep" הוא יעד אכיפה מפורש ב-2026, ובמיוחד repurposing ל-AI.** ב-2026 רשות הגנת הפרטיות (PPA) אוכפת באופן פעיל נגד "data creep" — מצב שבו מידע צרכני מגויס-מחדש לפיתוח AI בלי הסכמה מחודשת וממוקדת לכך. זה נוגע ישירות לתרחיש שבו דאטה שנאסף לחיפוש דירות "מוזרם" גם למכירה ל-AI/רובוטיקה (בדיוק כמו המודל הקיים ב-Obrik) — כדאי לוודא שהסכמה למכירה/שימוש-משני מנוסחת בנפרד ומראש, לא רק בתנאי-שימוש גורפים. הרשות יכולה להטיל קנסות מנהליים עד כ-3.2 מיליון ש"ח על הפרות.

זו לא ייעוץ משפטי — שווה שיחה עם עו"ד פרטיות ישראלי לפני שבונים את שכבת המכירה בהיקף רציני, במיוחד עם אכיפה פעילה ב-2026.

---

# מפת דרכים מדורגת

| טווח | מה | תלוי ב־ |
|---|---|---|
| **מיידי (ימים)** | להכניס בפועל את `vibe` לנוסחת הניקוד; להוסיף שדה latency-to-decision ו-label positive/negative ללוג הקיים | כלום — רק קוד |
| **מהיר (שבועות)** | טבלת `rentch-user-profiles` + כלי `update_user_profile` לאריק; חיבור 3 נקודות המגע ללוג משותף; משקלים per-cohort במקום גלובלי-קבוע | schema פשוט, בלי ML |
| **בינוני (חודש–חודשיים)** | embeddings לדירות+משתמשים דרך Gemini Embedding 2; אינדקס S3 Vectors; kNN כמקור-מועמדים נוסף; onboarding + cold-start swipes | נפח-דאטה בסיסי, לא עצום |
| **ארוך-טווח (רבעון+)** | אימון LightGBM על הלוג המצטבר; A/B framework אמיתי; מסך שקיפות "מה Rently למד"; שכבת אנונימיזציה + מוצרי דאטה ראשונים | נפח אינטראקציות מספק + ייעוץ משפטי לפני מכירה |

---

# נספח: מקורות למידע העדכני

המידע הבא אומת מול מקורות חיצוניים בזמן כתיבת המסמך (יוני–יולי 2026) ואינו מהווה ייעוץ משפטי:

- **תיקון 13 לחוק הגנת הפרטיות** — IAPP, Chambers and Partners (Data Protection & Privacy 2026 – Israel), Safetica, Recording Law
- **Gemini Embedding 2** — Google Developers Blog, Google AI for Developers (ai.google.dev)
- **Amazon S3 Vectors** — AWS News Blog, AWS "What's New"

כדאי לבדוק שוב לפני יישום בהיקף רציני — הן הרגולציה והן המוצרים הטכנולוגיים (S3 Vectors, Gemini Embedding) עדיין באזור פעיל של שינויים.
